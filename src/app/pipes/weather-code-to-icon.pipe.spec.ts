import { WeatherCodeToIconPipe } from './weather-code-to-icon.pipe';

describe('WeatherCodeToIconPipe', () => {
  it('create an instance', () => {
    const pipe = new WeatherCodeToIconPipe();
    expect(pipe).toBeTruthy();
  });
});
